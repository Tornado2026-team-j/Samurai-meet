import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://Tornado2026-team-j.github.io',
  base: '/Samurai-meet',
  outDir: './dist',
  trailingSlash: 'ignore',
  integrations: [],
});
