# 進捗（コードと自動検証の事実ベース）

最終確認: 2026-08-31（`eb142789`、PR #37、および未コミットのチャットModeration受入差分）

この表は「実装がある」「自動テストが通る」「iPhone実機で一連の操作を確認した」「未実装」を分けるためのスナップショットです。自動テストやCI成功は、実機E2E・OS通知・本番公開経路の確認を意味しません。

## 検証スナップショット

- この確認で `frontend/` の `recruitment` / `notifications` / `chat` / `crypto` テストを実行し、49件成功・失敗0件。
- 未コミットのチャットModeration差分では、`backend/` の `go test -count=1 ./internal/chat ./internal/httpapi` が成功し、`frontend/` の `bun test tests/chat.test.ts tests/notifications.test.ts` は24件成功・失敗0件。
- `eb142789` 時点のPR #37最終CIは全通過。Go quality、PostgreSQL integration、Bun quality、依存・脆弱性・Secret/CodeQL系チェックを含む。途中の実行中スナップショットは最終結果として扱わない。
- ローカルGoテストは端末のGo build cacheへのアクセス拒否で起動できなかった。これはソース失敗ではなくローカル検証環境の制約で、CIのGo quality成功とは別に記録する。

## 機能別の状態

| 領域 | 実装済み | 自動検証済み | 実機未確認 | 未実装 |
| --- | --- | --- | --- | --- |
| 募集・応募・通知 | 募集作成・下書き・検索・応募・承認/辞退・応募取消・募集管理、通知一覧/既読/構造化ID遷移がある。根拠: `backend/internal/matching/`、`backend/internal/notification/`、`frontend/app/`、`frontend/services/notifications.ts`。 | `frontend/tests/recruitment.test.ts` と `frontend/tests/notifications.test.ts` は今回成功。JST正規化・検索条件は `backend/internal/matching/service_test.go`、ライフサイクルは `backend/internal/integration/matching_test.go` にある。 | 同一のiPhoneと公開APIで、募集作成→応募→承認/辞退→通知遷移→応募取消を完走するE2E。 | OSプッシュ通知（下記）。 |
| 言語・JST・UI | 表示言語と利用モードは別設定、日時はISO内部値と`Asia/Tokyo`固定。根拠: `frontend/tests/recruitment.test.ts`、`backend/internal/matching/service_test.go`、`frontend/tests/notifications.test.ts`。 | 今回のフロント49件にJST変換・日跨ぎ・言語に依存しない通知遷移を含む。 | 表示言語の即時切替だけは既存記録があるが、日時picker・safe area・戻る/再読込・通知遷移を含む全画面E2Eは未確認。 | なし。ただし未確認の見た目を完了扱いにしない。 |
| チャット本文・履歴・既読 | RESTの暗号文送信、履歴、既読、保持期間スイープ、レート制限、送信前Moderationがある。根拠: `backend/internal/chat/service.go`、`backend/internal/chat/retention.go`、`backend/internal/httpapi/chat_moderation.go`、`frontend/services/chat.ts`。 | 今回の `backend/internal/chat` / `backend/internal/httpapi` テスト、`frontend/tests/chat.test.ts` が成功。既読と保持期間の統合テストは `backend/internal/integration/chat_read_test.go` / `chat_retention_test.go` にある。 | native WebTransport接続を含む送信・受信・復帰・再接続。 | サーバー翻訳、端末間鍵共有・ローテーションによる厳密E2EE。現行の送信前Moderationは平文をサーバーとOpenAIへ送るため、厳密E2EEではない。 |
| チャット画像 | バックエンドは暗号文BLOBのアップロード、メッセージへの一回限りの関連付け、参加者のみの取得、ブロック拒否、未参照・保持期限後の削除を実装。根拠: `backend/internal/chat/attachment.go`、`backend/internal/httpapi/chat_attachment.go`、`backend/migrations/0034_chat_attachments.sql`。 | `backend/internal/integration/chat_attachment_test.go` は認可・ブロック・孤児削除・保持期限を対象にし、`eb142789` 時点のPostgreSQL integration最終CIは成功。 | 実端末での画像選択、暗号化、送受信、復号、失敗復帰。 | 現行`frontend/app/chat/[id].tsx`と`frontend/services/chat.ts`には添付の選択・暗号化・送受信・復号UIがない。添付ごとの鍵を相手端末へ安全に渡す契約も未実装。 |
| Key B | 端末Key-Bは端末署名とプロフィール画像の端末別鍵envelopeに使う。根拠: `frontend/services/crypto.ts`、`frontend/services/photos.ts`、`frontend/services/key-management.ts`。サーバーへ平文を保存しない。 | `frontend/tests/crypto.test.ts` は今回成功し、Key-B公開鍵・端末署名・画像鍵の端末/アカウント包みを確認する。 | Keychain/Secure Storage、端末移行、回復後の再ラップを実機で確認。 | チャット本文・チャット添付をKey Bで保護する実装はない。添付APIはAccess Tokenとチャット参加者認可のみで、プロフィール画像の端末proofは要求しない。 |
| WebTransport | バックエンドの唯一のリアルタイム配送はHTTP/3 WebTransport。旧`/api/v1/ws/chats/{id}`は410。TLS 1.3/UDP、Origin allowlist、Authorization headerの短命Chat Token、0-RTT mutation拒否、再検証、PostgreSQL fan-outを実装。根拠: `backend/internal/chat/quic*.go`、`backend/internal/httpapi/chat_ws.go`、`backend/API_SPEC.md`。RESTは同期・復旧経路として残る。 | `frontend/tests/chat.test.ts` はWebTransport tokenとネイティブ未対応時のREST同期を検証。PR #37のGo qualityは成功。 | UDP/TLS公開経路とnative Development BuildでのiOS送受信・復帰・複数端末。 | iOS/Androidの実WebTransportネイティブブリッジは未実装。Expo Goでは利用不可で、型上のアダプタ契約だけがある。 |
| OpenAI Moderation | `POST /chats/{id}/moderation` は参加者・accepted match・ブロック状態を先に認可し、最大2,000 runesの送信前平文だけをOpenAI Moderationsへ同期送信する。本文、provider応答、カテゴリ、scoreはDB・キュー・ログへ保存せず、クライアントにもdecision以外を返さない。`allowed`のときだけ後続で暗号化して`/messages`へ送る。根拠: `backend/internal/chat/moderation.go`、`backend/internal/httpapi/chat_moderation.go`、`frontend/services/chat.ts`。 | `backend/internal/chat/moderation_test.go` はOpenAI契約・timeout・キー未設定を、`backend/internal/httpapi/chat_moderation_test.go` は認証/参加者認可・サイズ上限・provider情報非開示を対象にする。今回のバックエンド2パッケージとフロント24件も成功。 | OpenAI設定済みの本番相当環境で、allowed / blocked / unavailable / timeout / network / 5xx の送信停止と画面表示を実機確認。 | 画像Moderation、翻訳時検知、通報証拠のHTTP受付/worker配線、運営キュー。`blocked`、`unavailable`、timeout、network、5xxはfail-closedで暗号文送信を開始せず、画面は理由を一般化して表示する。送信前平文をサーバーとOpenAIが処理するので、これは厳密E2EEではない。 |
| OS Push | アプリ内REST通知は実装済み。根拠: `backend/internal/notification/`、`frontend/services/notifications.ts`。 | 通知取得・既読・遷移は今回のフロントテストで成功。 | OS権限、バックグラウンド受信、タップ遷移。 | `expo-notifications`等のOSプッシュ配送、端末token登録、配信worker。 |
| iOS実機E2E | 画面とAPIのコード、ならびに自動テストは存在する。 | 上記の範囲のみ。 | 募集/応募/通知、JST、言語、画像、Key B、WebTransport、OS Pushを同一環境で通すE2Eは未確認。 | 実機E2E結果そのもの。 |

## 既存ドキュメントとの矛盾

次の記述はHEADの実装と矛盾するため、現状説明として参照しない。

- `docs/requirements.md` の「現行REST/ WebSocket、QUIC/WebTransportは予定・未実装」という説明。
- `docs/ai/system/current-status.md` の「WebSocketのみを発行」「QUIC/WebTransport未着手」という説明。
- `docs/human/README.md` と `docs/human/overview.md` の「リアルタイム配送/チャット画面が未実装」という説明。
- `backend/API_SPEC.md` と `docs/features/safety.md` の「サーバーAIのチャットModerationは実行しない／存在しない」という説明。これは今回の送信前Moderation受入差分と矛盾する。

リアルタイム配送の現行仕様は `backend/API_SPEC.md` の「6.7 チャット（REST同期 + HTTP/3 WebTransport）」および `backend/HANDOFF.md` を正とする。ただし、これらの仕様書にある「native moduleと実機UDP経路は別途E2E確認が必要」という留保は維持する。

また、`backend/TODO.md` の「チャット内写真送信と通知連携は実装済み」はバックエンド契約までの意味に限定する。現行フロントの画像選択・鍵共有・送受信UIは未実装であり、利用者向け機能としては完了ではない。
